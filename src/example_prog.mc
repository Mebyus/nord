type Shape struct {
    a: u32,
    b: u32,
}

fn calc_area(s: Shape) => u64 {
    var a: u64 = 0;
    const abc: str = "Hello, world!";
    print(abc);
    s.init();
    return a * b * 0x10 + 345100 + 'g' + '\n' + '\'';
}
